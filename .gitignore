# Dependencias
node_modules

# Build
dist
dist-ssr
*.local
*.tsbuildinfo

# Variables de entorno (nunca se commitea un .env real — ver DEPLOYMENT.md)
.env
.env.*
!.env.example

# Supabase local
supabase/.branches
supabase/.temp
supabase/snippets
supabase/functions/.env*

# Claude Code (config/skills locales, no forman parte del proyecto)
.claude
.agents
skills-lock.json

# Notas de contexto para Claude Code, no se comitea (acordado)
CLAUDE.md

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Sistema operativo
.DS_Store

# Logs
npm-debug.log*
*.log

# Testing
coverage
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Vercel
.vercel

# TestSprite: los .py generados y tmp/config.json incrustan las credenciales
# reales pasadas por additionalInstruction (ver AI/DECISIONS.md, 2026-08-08) en
# texto plano — nunca se commitea, todo el directorio es descartable/regenerable.
testsprite_tests/

# Volcados de la base de datos: contienen datos personales reales
# (incluidos los de menores en `dependents`) y el repositorio es publico.
# Nunca deben subirse a Git — ver AI/SECURITY.md.
backups/
